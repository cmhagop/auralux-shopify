{%- style -%}
  .aura-faq {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    background-color: #111111;
    color: #F5F5F0;
    position: relative;
  }

  .aura-faq::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(197, 160, 89, 0.12), transparent);
  }

  .aura-faq__header {
    text-align: center;
    margin-bottom: 8rem;
  }

  .aura-faq__label {
    display: block;
    text-transform: uppercase;
    letter-spacing: 0.5rem;
    font-size: 1rem;
    color: #C5A059;
    margin-bottom: 2rem;
    font-weight: 600;
  }

  .aura-faq__heading {
    margin-bottom: 0;
    letter-spacing: -0.03em;
    color: #F5F5F0;
  }

  .aura-faq__container {
    max-width: 75rem;
    margin: 0 auto;
  }

  .aura-faq__item {
    border-bottom: 1px solid rgba(245, 245, 240, 0.05);
  }

  .aura-faq__item:first-child {
    border-top: 1px solid rgba(245, 245, 240, 0.05);
  }

  .aura-faq__question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 3rem 0;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    font-family: var(--font-body-family);
    font-size: 1.55rem;
    letter-spacing: 0.02em;
    color: #F5F5F0;
    transition: opacity 0.3s ease;
    font-weight: 500;
  }

  .aura-faq__question:hover {
    opacity: 0.6;
  }

  .aura-faq__icon {
    width: 1rem;
    height: 1rem;
    transition: transform 0.5s cubic-bezier(0.19, 1, 0.22, 1);
    opacity: 0.3;
    flex-shrink: 0;
    margin-left: 2rem;
    color: #C5A059;
  }

  .aura-faq__item[open] .aura-faq__icon {
    transform: rotate(180deg);
  }

  .aura-faq__answer {
    padding-bottom: 3.5rem;
    font-size: 1.5rem;
    line-height: 1.9;
    color: rgba(245, 245, 240, 0.5);
    max-width: 60rem;
    animation: faqReveal 0.5s cubic-bezier(0.19, 1, 0.22, 1);
  }

  @keyframes faqReveal {
    from { opacity: 0; transform: translateY(-8px); }
    to { opacity: 1; transform: translateY(0); }
  }
{%- endstyle -%}

<section class="aura-faq">
  <div class="page-width">
    <div class="aura-faq__header">
       <span class="aura-faq__label">Common Inquiries</span>
       <h2 class="aura-faq__heading h1">{{ section.settings.heading | escape }}</h2>
    </div>

    <div class="aura-faq__container">
      {%- for block in section.blocks -%}
        <details class="aura-faq__item" {{ block.shopify_attributes }}>
          <summary class="aura-faq__question">
            <span>{{ block.settings.question | escape }}</span>
            <span class="aura-faq__icon">{% render 'icon-caret' %}</span>
          </summary>
          <div class="aura-faq__answer">
            {{ block.settings.answer }}
          </div>
        </details>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Aura Lux FAQ",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Elegance Explained"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 100
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 100
    }
  ],
  "blocks": [
    {
      "type": "faq_item",
      "name": "FAQ Item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question",
          "default": "How do I determine my ring size?"
        },
        {
          "type": "richtext",
          "id": "answer",
          "label": "Answer",
          "default": "<p>We offer a complimentary sizing kit for all orders. Alternatively, you can visit our sizing guide for detailed instructions on measuring at home.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Aura Lux FAQ",
      "blocks": [
        { "type": "faq_item" },
        { "type": "faq_item", "settings": { "question": "What is the delivery timeline?", "answer": "<p>Each piece is handcrafted to order. Standard delivery takes 2-4 weeks, ensuring the highest level of detail for your Aura Ring.</p>" } },
        { "type": "faq_item", "settings": { "question": "Are your materials ethically sourced?", "answer": "<p>Sustainability is at the core of Aura Lux. We use 100% recycled precious metals and conflict-free diamonds certified by the Kimberley Process.</p>" } }
      ]
    }
  ]
}
{% endschema %}
